{% extends 'base.html.twig' %}

{% block title %}Accueil - Gestion des Équipements Universitaires{% endblock %}

{% block stylesheets %}
    <style>
        body {
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #2196f3 0%, #43e97b 100%, #fff 100%);
            font-family: 'Segoe UI', Arial, sans-serif;
        }
        .home-container {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(33, 150, 243, 0.08), 0 1.5px 6px rgba(67, 233, 123, 0.08);
            max-width: 600px;
            margin: 60px auto;
            padding: 32px 28px 28px 28px;
        }
        .home-title {
            font-size: 2.2rem;
            font-weight: bold;
            margin-bottom: 18px;
            text-align: center;
            color: #2196f3;
        }
        .home-desc {
            font-size: 1.1rem;
            text-align: center;
            margin-bottom: 22px;
            color: #333;
        }
        .home-list {
            color: #444;
            margin-bottom: 30px;
        }
        .home-list li {
            margin-bottom: 8px;
        }
        .home-btn {
            display: inline-block;
            background: linear-gradient(90deg, #2196f3 60%, #43e97b 100%);
            color: #fff;
            padding: 12px 32px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 500;
            transition: background 0.2s;
        }
        .home-btn:hover {
            background: linear-gradient(90deg, #43e97b 60%, #2196f3 100%);
        }
    </style>
{% endblock %}

{% block body %}
<div class="home-container">
    <div class="home-title">Bienvenue sur l'application de gestion et réservation des équipements</div>
    <div class="home-desc">
        à l'université Don Bosco de Lubumbashi (UDBL)
    </div>
    <ul class="home-list">
        <li>Réservez facilement vos équipements universitaires</li>
        <li>Suivi en temps réel de l'état des équipements</li>
        <li>Gestion centralisée par l'administrateur</li>
    </ul>
    <a href="{{ path('login') }}" class="home-btn">Se connecter</a>
</div>
{% endblock %}
